def add(a,b):
    """
    add函数可以接受两个参数，进行两数相加
    :param a:参数中其中一个
    :param b:另一个
    :return:返回值是两数字相加的结果
    """
    result = a+b
    print(f"两数相加的结果是：{result}")
    return result
add(5,6)
